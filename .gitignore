# Dependencies
/node_modules
/.pnp
pnp.js

# Testing
/coverage

# Next.js build output
/.next
/out

# Production build
/build

# Miscellaneous
.DS_Store
*.pem

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
env
env.local
env.development.local
env.test.local
env.production.local

# Vercel deployment
vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# MacOS
.DS_Store

# Agent instructions live in AGENTS.md; Claude Code reads CLAUDE.md, so create
# a local symlink once per clone: ln -s AGENTS.md CLAUDE.md
CLAUDE.md

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log
# keep .terraform.lock.hcl committed; ignore local var files (may contain PII/emails)
*.tfvars
!*.tfvars.example
override.tf
override.tf.json
*_override.tf
*_override.tf.json